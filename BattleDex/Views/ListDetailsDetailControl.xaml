<UserControl
    x:Class="BattleDex.Views.ListDetailsDetailControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:helpers="using:BattleDex.Helpers"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d">
    <UserControl.Resources>
        <helpers:PokemonTypeToColorConverter x:Key="TypeToColorConverter" />
        <helpers:TypeMultiplierToDisplayConverter x:Key="TypeMultiplierToDisplayConverter" />
        <helpers:TypeMultiplierToColorConverter x:Key="TypeMultiplierToColorConverter" />
        <helpers:PokemonTypeToForegroundConverter x:Key="TypeToForegroundConverter" />
        <helpers:TypeMultiplierToForegroundConverter x:Key="TypeMultiplierToForegroundConverter" />
    </UserControl.Resources>
    <ScrollViewer
        Name="ForegroundElement"
        HorizontalAlignment="Stretch"
        VerticalScrollMode="Enabled"
        IsTabStop="True">
        <StackPanel HorizontalAlignment="Left" Padding="12,0,0,24">
            <Grid HorizontalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Image
                    Grid.Column="0"
                    Width="96"
                    Height="96"
                    VerticalAlignment="Center"
                    Source="{x:Bind DisplayedSpriteUri, Mode=OneWay}"
                    Stretch="Uniform" />
                <ToggleButton
                    Grid.Column="1"
                    Width="32"
                    Height="32"
                    Padding="0"
                    Margin="4,0,0,0"
                    VerticalAlignment="Top"
                    Background="Transparent"
                    BorderThickness="0"
                    IsChecked="{x:Bind IsShiny, Mode=TwoWay}"
                    ToolTipService.ToolTip="Toggle shiny sprite">
                    <PathIcon
                        Width="14"
                        Height="14"
                        Data="M7,3 Q7,8 12,8 Q7,8 7,13 Q7,8 2,8 Q7,8 7,3 Z M11.5,1.5 Q11.5,3 13,3 Q11.5,3 11.5,4.5 Q11.5,3 10,3 Q11.5,3 11.5,1.5 Z" />
                </ToggleButton>
                <StackPanel
                    Grid.Column="2"
                    Margin="{StaticResource SmallLeftMargin}"
                    VerticalAlignment="Center"
                    Orientation="Horizontal">
                    <TextBlock
                        Text="{x:Bind ListDetailsMenuItem.NameEnglish, Mode=OneWay}"
                        Style="{ThemeResource TitleTextBlockStyle}" />
                    <TextBlock
                        Margin="12,0,0,0"
                        VerticalAlignment="Bottom"
                        Padding="0,0,0,2"
                        Text="{x:Bind ListDetailsMenuItem.IdDisplay, Mode=OneWay}"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        FontSize="18" />
                </StackPanel>
            </Grid>
            <StackPanel Padding="0,15,0,0">
                <TextBlock
                    Text="French Name"
                    Style="{StaticResource DetailSubTitleStyle}" />
                <TextBlock Text="{x:Bind ListDetailsMenuItem.NameFrench, Mode=OneWay}" Style="{StaticResource DetailBodyBaseMediumStyle}" />

                <TextBlock
                    Margin="{StaticResource SmallTopMargin}"
                    Text="Types"
                    Style="{StaticResource DetailSubTitleStyle}" />
                <ItemsControl ItemsSource="{x:Bind ListDetailsMenuItem.Types, Mode=OneWay}" Margin="0,6,0,0">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal" Spacing="8" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border
                                Background="{Binding Converter={StaticResource TypeToColorConverter}}"
                                CornerRadius="4"
                                Padding="12,4">
                                <TextBlock
                                    Text="{Binding}"
                                    Foreground="{Binding Converter={StaticResource TypeToForegroundConverter}}"
                                    FontWeight="SemiBold"
                                    FontSize="12" />
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <TextBlock
                    Margin="{StaticResource SmallTopMargin}"
                    Text="Abilities"
                    Style="{StaticResource DetailSubTitleStyle}" />
                <TextBlock Text="{x:Bind ListDetailsMenuItem.AbilitiesDisplay, Mode=OneWay}" Style="{StaticResource DetailBodyBaseMediumStyle}" />

                <TextBlock
                    Margin="{StaticResource SmallTopMargin}"
                    Text="EV Yield"
                    Style="{StaticResource DetailSubTitleStyle}" />
                <TextBlock x:Name="EvYieldText" Style="{StaticResource DetailBodyBaseMediumStyle}" />

                <!-- Weaknesses -->
                <TextBlock
                    Margin="{StaticResource SmallTopMargin}"
                    Text="Weaknesses"
                    Style="{StaticResource DetailSubTitleStyle}" />
                <ItemsControl x:Name="WeaknessesControl" ItemsSource="{x:Bind CurrentMatchup.Weaknesses, Mode=OneWay}" Margin="0,6,0,0">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <controls:WrapPanel Orientation="Horizontal" HorizontalSpacing="8" VerticalSpacing="8" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border
                                Background="{Binding Converter={StaticResource TypeMultiplierToColorConverter}}"
                                CornerRadius="4"
                                Padding="12,4">
                                <TextBlock
                                    Text="{Binding Converter={StaticResource TypeMultiplierToDisplayConverter}}"
                                    Foreground="{Binding Converter={StaticResource TypeMultiplierToForegroundConverter}}"
                                    FontWeight="SemiBold"
                                    FontSize="12" />
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Resistances -->
                <TextBlock
                    Margin="{StaticResource SmallTopMargin}"
                    Text="Resistances"
                    Style="{StaticResource DetailSubTitleStyle}" />
                <ItemsControl x:Name="ResistancesControl" ItemsSource="{x:Bind CurrentMatchup.Resistances, Mode=OneWay}" Margin="0,6,0,0">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <controls:WrapPanel Orientation="Horizontal" HorizontalSpacing="8" VerticalSpacing="8" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border
                                Background="{Binding Converter={StaticResource TypeMultiplierToColorConverter}}"
                                CornerRadius="4"
                                Padding="12,4">
                                <TextBlock
                                    Text="{Binding Converter={StaticResource TypeMultiplierToDisplayConverter}}"
                                    Foreground="{Binding Converter={StaticResource TypeMultiplierToForegroundConverter}}"
                                    FontWeight="SemiBold"
                                    FontSize="12" />
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Immunities -->
                <TextBlock
                    x:Name="ImmunitiesHeader"
                    Margin="{StaticResource SmallTopMargin}"
                    Text="Immunities"
                    Style="{StaticResource DetailSubTitleStyle}"
                    Visibility="{x:Bind HasImmunities, Mode=OneWay}" />
                <ItemsControl x:Name="ImmunitiesControl" ItemsSource="{x:Bind CurrentMatchup.Immunities, Mode=OneWay}" Margin="0,6,0,0"
                    Visibility="{x:Bind HasImmunities, Mode=OneWay}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <controls:WrapPanel Orientation="Horizontal" HorizontalSpacing="8" VerticalSpacing="8" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border
                                Background="{Binding Converter={StaticResource TypeToColorConverter}}"
                                CornerRadius="4"
                                Padding="12,4">
                                <TextBlock
                                    Text="{Binding}"
                                    Foreground="{Binding Converter={StaticResource TypeToForegroundConverter}}"
                                    FontWeight="SemiBold"
                                    FontSize="12" />
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>
